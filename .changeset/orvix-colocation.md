---
"orvix-copilot-chat": patch
---

Refactor the reasoning-effort request gating into a vscode-free helper (`src/provider/effort.ts`) so it can be covered by the colocated `node` test runner. No behavior change.